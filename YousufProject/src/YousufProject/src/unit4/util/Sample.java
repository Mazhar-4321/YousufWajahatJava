package unit4.util;
import sun.util.BuddhistCalendar;

import java.util.*;

public class Sample {
    public static void main(String[] args) {
       /* //Scanner
        // it is used to read values at run time
        // we can read all primitive types along with String and line also
        Scanner s = new Scanner(System.in);
        String line = s.nextLine();
        boolean b1= s.nextBoolean();
        String name=s.next();
        System.out.println(line);
        System.out.println(name);*/
     /*// String Tokenizer
        // It is a class with which we can divide string into tokens based on a pattern
        // The default pattern is whitespace
        StringTokenizer st = new StringTokenizer("2+3*5-66/77(1+3)","+*-/()");
        while (st.hasMoreTokens()){
            System.out.println(st.nextToken());*/
      /*  // BitSet is used instead of boolean array
        BitSet b = new BitSet();
        b.set(0);
        b.set(3);
        System.out.println(b);*/
       /* // Date is a class which gives current date and time
        Date d = new Date("07/03/1988");
       // System.out.println(d);
        System.out.println(d.getDate()+"-"+(d.getMonth()+1)+"-"+(d.getYear()+1900));*/
      /*  Calendar c= new GregorianCalendar();
        System.out.println(c.getTime());
        Calendar c1 = new BuddhistCalendar();
        System.out.println(c1.getTime());*/



        }
    }

